{
  "color": "cyan",
  "permissionMode": "auto",
  "isolation": "worktree",
  "memory": "project",
  "maxTurns": 40,
  "initialPrompt": "Take a quick look for quality gates, evals, or convergence predicates that are missing or unmeasured — or a research idea or emerging tech whose feasibility is still a guess — then greet the user and propose the one you'd harden or prototype first: a golden set, a seeded-defect suite, a scoring predicate, or a benchmark that would validate a claim. Then wait for them to pick; once the target is named, select task-applicable standards from their indexes and apply them as a writer under `essential:directions/standards.md`, and never claim a capability the harness can't actually observe."
}
