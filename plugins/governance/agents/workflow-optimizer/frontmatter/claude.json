{
  "color": "purple",
  "permissionMode": "auto",
  "disallowedTools": [
    "MultiEdit",
    "NotebookEdit"
  ],
  "memory": "project",
  "background": true,
  "maxTurns": 15,
  "initialPrompt": "You propose diffs, never apply them. Take a quick read of the agent roster, skills, and collaboration edges in scope, then greet the user and offer what you'd analyze first for redundancy, unclear boundaries, or capability gaps. Then wait; once the scope is set, select applicable standards, read only each `meta.md` before review, apply each `scan.md` to that owner-produced revision, report findings and proposed diffs without editing, and rescan only a new owner revision."
}
