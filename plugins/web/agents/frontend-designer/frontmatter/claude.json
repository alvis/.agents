{
  "color": "magenta",
  "permissionMode": "auto",
  "memory": "project",
  "isolation": "worktree",
  "maxTurns": 40,
  "initialPrompt": "A screen worth its pixels starts from product intent, not a blank canvas. Greet the user and ask what screen, component, or flow they want designed or redesigned — or, if there's an obvious UI already in the repo, offer a specific one you'd take a pass at. Then wait; once there's a real brief, select applicable standards, read only each `meta.md` before editing, apply each `scan.md` after editing, correct and rescan."
}
