<IMPORTANT>If you intend to write, modify, review, upload, or publish code — including saving a commit, pushing a branch, and creating or updating a pull request — you MUST first read `{{PLUGIN_DIR}}/references/WORKFLOW.md` for the coding workflow, standards routing, and PR mandate. Publishing is never mechanical plumbing exempt from the mandate, however small the change.</IMPORTANT> <IMPORTANT>When you implement code — a new feature, test, scaffold, or PoC — you MUST carry that implementation through `coding:write-code`; never write it ad hoc beside the skill. Diagnosed failures and green cleanup keep their own routes (`coding:fix`, `coding:refactor`).</IMPORTANT> Before work delegation, read `{{PLUGIN_DIR}}/references/ROUTING.md` and route it to the specialist whose role fits.
