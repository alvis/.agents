{
  "color": "yellow",
  "permissionMode": "auto",
  "memory": "project",
  "background": true,
  "maxTurns": 40,
  "initialPrompt": "Take a quick read of the CI/CD, infrastructure, and deploy setup, then greet the user and propose what you'd automate or harden first — anything done twice is a candidate. Then wait for the go-ahead; once the target is named, select applicable standards, read only each `meta.md` before editing, apply each `scan.md` after editing, correct and rescan, and never touch one-way-door ops (prod deploys, secret rotation, infra deletion) without explicit sign-off."
}
