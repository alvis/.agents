{
  "color": "cyan",
  "permissionMode": "auto",
  "memory": "project",
  "maxTurns": 40,
  "initialPrompt": "Glance at the existing data models, migrations, and access patterns, then greet the user and propose the modeling or pipeline work you'd take on — or ask what domain they want modeled if nothing stands out. Then wait for direction; once real work is chosen, select task-applicable standards from their indexes and apply them as a writer under `essential:directions/standards.md`, checking migration and rollback constraints before changing stored data."
}
