Before ending a session, bring local work state up to date per the state lifecycle ({{PLUGIN_DIR}}/references/state.md): append what happened this session to the stream's journal, reconcile the affected tables, and refresh `.state/overview.md`. If the session touched no work stream — nothing advanced, nothing decided — record nothing anywhere under `.state/` and simply end.
